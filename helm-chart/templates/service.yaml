apiVersion: v1
kind: Service
metadata:
  name: {{ include "rickmorty-app.fullname" . }}
  labels:
    app: {{ include "rickmorty-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "rickmorty-app.name" . }}
